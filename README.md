# zcashd-log-cleaner
A script that removes some escape sequences from a file and rewrites a clean log.
Place and run alongside (in the same dir as) zcashd.log. 
The output file is named zcashd2.log, respectively.
These two filepath names are declared on lines 9 and 11 of main.rs.
